
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an application user with access to the system. Authentication data (passwords) are handled by an external system. Only specific users (TAC members) will have permission to mark technician availability.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "username": {
          "type": "string",
          "description": "The unique username used for login."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "roleId": {
          "type": "string",
          "description": "Reference to the Role entity, defining the user's permissions within the application. (Relationship: Role 1:N User)"
        }
      },
      "required": [
        "id",
        "username",
        "roleId"
      ]
    },
    "Role": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Role",
      "type": "object",
      "description": "Defines the different types of users or roles within the application, such as 'TAC Member' (who can edit schedules) and 'Viewer' (who can only view schedules).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Role entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the role (e.g., 'TAC Member', 'Viewer')."
        },
        "description": {
          "type": "string",
          "description": "A detailed explanation of what this role represents and its associated privileges."
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "Technician": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Technician",
      "type": "object",
      "description": "Represents a technical staff member whose schedule and availability are managed within the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Technician entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the technician."
        },
        "phoneTariff": {
          "type": "string",
          "description": "The technician's work phone number (Tarifa)."
        },
        "phonePersonal": {
          "type": "string",
          "description": "The technician's personal phone number."
        },
        "workRegime": {
          "type": "string",
          "description": "The technician's work schedule type (e.g., 'Comercial', '12x36').",
          "enum": ["Comercial", "12x36"]
        },
        "isOnDuty": {
          "type": "boolean",
          "description": "Indicates if the technician is currently on call (plantão)."
        }
      },
      "required": [
        "id",
        "name",
        "workRegime"
      ]
    },
    "ScheduledBlock": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ScheduledBlock",
      "type": "object",
      "description": "Represents a specific block of time when a technician is marked as unavailable. Supports multiple teams in the same slot.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ScheduledBlock entity."
        },
        "technicianId": {
          "type": "string",
          "description": "Reference to the Technician who is unavailable."
        },
        "startTime": {
          "type": "string",
          "description": "The start time of the block.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The end time of the block.",
          "format": "date-time"
        },
        "equipe1": {
          "type": "boolean",
          "description": "Indicates if Equipe 1 is scheduled."
        },
        "equipe2": {
          "type": "boolean",
          "description": "Indicates if Equipe 2 is scheduled."
        },
        "markedByUserId": {
          "type": "string",
          "description": "User who marked this block."
        }
      },
      "required": [
        "id",
        "technicianId",
        "startTime",
        "endTime"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/roles/{roleId}",
        "definition": {
          "entityName": "Role",
          "schema": {
            "$ref": "#/backend/entities/Role"
          },
          "description": "Stores definitions of user roles.",
          "params": [
            {
              "name": "roleId",
              "description": "The unique identifier of the role."
            }
          ]
        }
      },
      {
        "path": "/technicians/{technicianId}",
        "definition": {
          "entityName": "Technician",
          "schema": {
            "$ref": "#/backend/entities/Technician"
          },
          "description": "Stores profiles of technical staff.",
          "params": [
            {
              "name": "technicianId",
              "description": "The unique identifier of the technician."
            }
          ]
        }
      },
      {
        "path": "/technicians/{technicianId}/scheduledBlocks/{scheduledBlockId}",
        "definition": {
          "entityName": "ScheduledBlock",
          "schema": {
            "$ref": "#/backend/entities/ScheduledBlock"
          },
          "description": "Stores specific time blocks.",
          "params": [
            {
              "name": "technicianId",
              "description": "The unique identifier of the technician."
            },
            {
              "name": "scheduledBlockId",
              "description": "The unique identifier of the scheduled block."
            }
          ]
        }
      },
      {
        "path": "/roles_tac_members/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Identifies users who are 'TAC Members'.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      }
    ]
  }
}
